问题：给定一个有根数，问计算每层有多少叶子节点。

方法：广度搜索，用一个levelcnt[2]的数组保存相邻两层的节点数，用一个cur在0和1之间转换
，初始时cur=0，levelcnt[cur]为1，仅有根节点，levelcnt[cur+1]为0，还没有统计。
每遍历一个节点，levelcnt[cur]--,每找到一个孩子节点levelcnt[(cur+1)%2]++,当levelcnt[cur]==0时，一层遍历完毕，cur = （cur+1）%2，遍历下一层。
如果在某层中找到一个没有孩子节点的节点，则该层叶子节点数加1